<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="warn">
    <contextName>logback</contextName>
    <!-- 应用名称 -->
    <Property name="app_name" value="lexin-business"/>
    <!-- 应用Java包 -->
    <Property name="app_package" value="com.thinkerjet.business"/>
    <!-- 日志打印格式 -->
    <Property name="patternLayout" value="%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{50}-%M:%line - %msg%n"/>
    <!--日志文档保留天数-->
    <Property name="max_history" value="60"/>
    <!--每个文件大小-->
    <Property name="max_file_size" value="50MB"/>
    <!--日志文件的总大小-->
    <Property name="total_size_cap" value="10GB"/>
    <Property name="encoding" value="UTF-8"/>

    <!-- 应用日志存储路径 -->
    <springProfile name="dev,test">
        <Property name="log_path" value="/data/webapp/logs/${app_name}"/>
    </springProfile>
    <springProfile name="prod">
        <Property name="log_path" value="/home/webapp/logs/${app_name}"/>
    </springProfile>
    <!-- 归档日志路径-->
    <Property name="archive_log_path" value="${log_path}/archive-logs"/>

    <!--  输出到控制台  -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>DEBUG</level>
        </filter>
        <encoder>
            <pattern>${patternLayout}</pattern>
            <charset>${encoding}</charset>
        </encoder>
    </appender>

    <!--
       开发环境输出到控制台(包含sql信息)
       测试环境输出到控制台和文件（包含sql信息）
       生产环境输出到控制台和文件
    -->
    <springProfile name="dev">
        <!--指定特定路径下的日志输出为debug模式。这里是指定持久层操作打印出sql-->
        <logger name="${app_package}.dao" level="DEBUG"/>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <springProfile name="test">
        <appender name="INFO-APPENDER" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${log_path}/${app_name}-info.log</file>
            <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
                <level>DEBUG</level>
            </filter>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>${archive_log_path}/${app_name}-info.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
                <maxFileSize>${max_file_size}</maxFileSize>
                <maxHistory>${max_history}</maxHistory>
                <totalSizeCap>${total_size_cap}</totalSizeCap>
            </rollingPolicy>
            <encoder>
                <pattern>${patternLayout}</pattern>
                <charset>${encoding}</charset>
            </encoder>
        </appender>

        <appender name="ERROR-APPENDER" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${log_path}/${app_name}-error.log</file>
            <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
                <level>ERROR</level>
            </filter>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>${archive_log_path}/${app_name}-error.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
                <maxFileSize>${max_file_size}</maxFileSize>
                <maxHistory>${max_history}</maxHistory>
                <totalSizeCap>${total_size_cap}</totalSizeCap>
            </rollingPolicy>
            <encoder>
                <pattern>${patternLayout}</pattern>
                <charset>${encoding}</charset>
            </encoder>
        </appender>

        <!--指定特定路径下的日志输出为debug模式。这里是指定持久层操作打印出sql-->
        <logger name="${app_package}.dao" level="DEBUG" additivity="false">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="INFO-APPENDER"/>
        </logger>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="INFO-APPENDER"/>
            <appender-ref ref="ERROR-APPENDER"/>
        </root>
    </springProfile>

    <springProfile name="prod">
        <appender name="INFO-APPENDER" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${log_path}/${app_name}-info.log</file>
            <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
                <level>INFO</level>
            </filter>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>${archive_log_path}/${app_name}-info.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
                <maxFileSize>${max_file_size}</maxFileSize>
                <maxHistory>${max_history}</maxHistory>
                <totalSizeCap>${total_size_cap}</totalSizeCap>
            </rollingPolicy>
            <encoder>
                <pattern>${patternLayout}</pattern>
                <charset>${encoding}</charset>
            </encoder>
        </appender>

        <appender name="ERROR-APPENDER" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${log_path}/${app_name}-error.log</file>
            <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
                <level>ERROR</level>
            </filter>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>${archive_log_path}/${app_name}-error.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
                <maxFileSize>${max_file_size}</maxFileSize>
                <maxHistory>${max_history}</maxHistory>
                <totalSizeCap>${total_size_cap}</totalSizeCap>
            </rollingPolicy>
            <encoder>
                <pattern>${patternLayout}</pattern>
                <charset>${encoding}</charset>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="INFO-APPENDER"/>
            <appender-ref ref="ERROR-APPENDER"/>
        </root>
    </springProfile>

</Configuration>
